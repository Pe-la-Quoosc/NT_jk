pipeline {
    agent any

    stages {

        stage('Clone') {
            steps {
                echo 'Cloning source code...'
            }
        }

        // stage('Build Docker Image') {
        //     steps {
        //         sh 'docker build -t devops-demo .'
        //     }
        // }

        // stage('Run Container') {
        //     steps {
        //         sh 'docker rm -f devops-container || true'
        //         sh 'docker run -d --name devops-container -p 3000:3000 devops-demo'
        //     }
        // }

    }
}